function book(title, author, pagenumber, ages, genre) {
  this.title = title;
  this, (author = author);
  this.pagenumber = pagenumber;
  this.ages = ages;
  this.genre = genre;
}
function moto(model, plate, wheels, instructions) {
  this.model = model;
  this.plate = plate;
  this.wheels = wheels;
  this.instructions = instructions;
}
